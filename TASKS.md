# webpic — TASKS

Check off `[ ]` → `[x]` as steps land. The `/tasks` skill summarizes progress; `/task` reads this file.
Full rationale for every step: **`docs/DESIGN.md`** (read on demand — not auto-loaded).
Completed items are summarized to the load-bearing decisions; the git log is the full audit trail.

**v0.1 = M0–M4 + M6.** v0.1 deliberately skips M5 (particles → v0.2): the sequence is M4 → M6.
**Current milestone: M3** (M2 complete — volume + perf gate closed at the 256³ v0.1 ceiling; see the M2 exit gate).

> **Scope-risk decision point — M2 exit:** if M2 slips materially, first cut
> **M4 (streamlines)** to v0.2 — slice + volume is already a useful viewer. Cut M6 only if the Zarr
> writer / `attrs["reduction"]` round-trip proves harder than expected (export gates the analyzer).

---

## M0 — Foundation
Goal: toolchain + schema codegen + scaffolds boot; OffscreenCanvas worker alive.
- [x] 1. Vite + TS strict (`tsconfig.base.json`) + Biome + Vitest; `npm` scripts wired
- [x] 2. `src/<layer>/` skeleton (schema, containers, coordinates, numerics, gpu) + path aliases (`@schema/*`, …)
- [x] 3. `scripts/check-boundaries.ts` (ts-morph) — layer-DAG enforcement, runs in CI ahead of `biome ci`
- [x] 4. Schema codegen: `scripts/codegen/{bundle,emit,render-schema,render-aliases,render-recipes,render-registry}.ts` (pypic `export bundle` → Zod + TS types via `npm run gen`)
- [x] 5. `gpu/`: device acquire, capability probe, `device.lost` recovery, timestamp-query + `performance.now` fallback profiler
- [x] 6. OffscreenCanvas-on-Worker scaffold (`transferControlToOffscreen`); main-vs-worker frame-parity test
- [x] 7. Theme loader — reads `pypic/plotting/themes/*.toml` `[webpic]` block (7 themes, incl. `anuppuccin-light`)
- [x] 8. OPFS cache w/ `navigator.locks`; writes confined to `data.worker.ts` (worker-only sync handle; Safari <26)
- [x] 9. Background backend microbench (seeds dispatcher calibration; scores cached in OPFS by GPU adapter)
- [x] **Exit gate:** cold-start page paint <500 ms; first frame <1500 ms (M2 Pro Chrome stable) — **met** on base M2 (146 ms / 210 ms, via `npm run perf:gate`). Re-validate at M2 once the raymarcher + `compileAsync`-at-boot replace the scaffold triangle.

## M1 — Static slice
Goal: read a Zarr store and render one themed orthogonal slice of `|B|`.
- [x] 1. `data/readers/zarr.ts` (zarrita.js) implementing `SimulationReader` + `FieldListingReader`
- [x] 2. `data/readers/_registry.ts` — `registerReader` + `openSimulation()` (confidence-ranked)
- [x] 3. `data/stagger.ts` — destagger Yee components to the co-located grid on load (`destaggerToColocated`, wired into the Zarr reader behind a no-op fast path); typed-array-backed `FieldDataset` consumed verbatim
- [x] 4. `derived/magnitude.ts` registered under canonical `'|B|'`; TS backend in `compute/backends/ts`
- [x] 5. `render`: one orthogonal `Mesh` (`NodeMaterial` sampling `uVolume`) with themed colormap
- [x] 6. `computeField('|B|', dataset)` wired store → compute → render end-to-end
- [x] 7. Schema-parity test (`pypic schema diff` + regenerated-vs-checked-in Zod) + additive-compat test
- [x] **Exit gate:** `computeField('|B|')` end-to-end; schema-parity + additive-compat green vs live pypic (`WEBPIC_PYPIC_PARITY=1`) — **met**.

## M2 — Volume + perf gate
Goal: single-scalar volume raymarcher hitting the perf gate; time-series scrub.
- [x] 1. Single-pass volume raymarcher (TSL `NodeMaterial` + `wgslFn`, render-local — not a shared kernel); analytic ray-box, early-α (≥0.98) termination. `render/raymarchScene.ts` (TSL `Loop` + `wgslFn` `hitBox` + front-to-back composite); `rayBox.ts` TS twin + Node tests. Slice stays app default until the M2.2 UI toggle.
- [x] 2. **`ui/` scaffold** — hideable shell panels + dependency-free `Pane`/`Folder`/`Binding` controls facade (slider/select/checkbox/text, ported from `magviz/src/ui/controls`, **no `tweakpane`**) on a callbacks-out / `set()`-in contract with `ownerDocument` DI. `ui` dispatches typed store intents only — never imports `render` (DAG-enforced); embed renders without `@webpic/ui`. `installUi()` mounts a themed docked shell + field selector (`selectField` intent) + `layers`/`diagnostics` placeholders + `toggleUi` shortcut. (Full `RangeControl` → M2.3.)
- [x] 3. Transfer-function texture (256×1 `rgba16float`) + window/level via **owned `RangeControl`** (ported from magviz: full linear/log/symlog/interval + `rangeMath`) — its `{center,width}` output is the canonical range form M2.5b's `ColormapBinding` stores (no separate `min/max`). `render/transferFunction.ts` bakes the colormap LUT (RGB; alpha reserved for the M2.7 opacity TF); window/level become `uWindowCenter`/`uWindowWidth` uniforms with an in-place `setWindowLevel` so dragging never re-uploads the volume. The **Layers** placeholder becomes a real window/level panel (M2.5b adds colormap + scale pick).
- [x] 4a. **Render loop + store-owned camera pose** — lift the camera out of the scene factories into `render/camera.ts` (`applyPose` + `DEFAULT_POSE`); pose lives in the store (orbit-spherical `{target, azimuth, elevation, distance}`) and streams to the worker as a `setCameraPose` `RenderWorkerRequest` (DESIGN §Worker message protocol). **No gl-matrix** — the `@schema` readonly `Vec3` tuple already crosses store↔render and suits zustand's reference-equality (gl-matrix deferred to M4 per CLAUDE.md). Worker drives an **on-demand rAF loop** (`needsRender` dirty flag; synchronous-paint fallback under Node) replacing per-message `renderOnce`; a `readbackInFlight` guard protects the test-only readback. Pose drives the **perspective/volume camera only**, so it's inert on the default ortho slice until 4b + the volume toggle. Unblocks honest per-frame perf (M2.8 + exit gate) and the M2.13 HMR keep-pose prerequisite.
- [x] 4b. **Orbit/turntable controls + pointer input** — orbit/dolly/pan as **pure pose helpers** in `store/camera.ts` (`orbitPose`/`dollyPose`/`panPose`, fresh-object-per-call for `subscribeWithSelector`). `ui/pointerCamera.ts` (`ui → store` only) attaches pointer/wheel listeners to the main-thread `<canvas>` — `transferControlToOffscreen` moves only the drawing surface, so the element still gets DOM events — dispatching `setCameraPose` (`RenderWorkerRequest`); wired **guarded** so the headless test's fake canvas is untouched. Live HUD `ui/cameraChrome.ts`: bottom-left readout + a CSS-3D gnomon driven straight from the pose (no second renderer). Render side unchanged (4a's rAF loop coalesces a drag into one paint); inert-until-volume like 4a. **Deferred:** the `navMode` selector + free-trackball orbit (needs a quaternion pose), and the **projection toggle** (needs a matched-frustum ortho camera; rides the M2.5a compositor). **Follow-on (post-checkoff):** fly-through dolly (wheel past the near limit walks the pivot forward), camera **roll** (Shift+Q/E bank, Shift+R level — `roll` added to `CameraPose`/`DEFAULT_POSE`), **axis-snap hotkeys** (digits 1–6/0 jump to ±axis views), and a **help overlay** (`?`/`H`) backed by an authoritative `ui/shortcuts.ts` registry.
- [x] 5a. **Layer model** (instance-first foundation) — store `layers` registry: ordered `{id, kind: volume|slice|fieldlines|particles, inputs, colormapBindingId, visible, opacity, params}` + intents (add/remove/select/reorder/setVisible/setOpacity), in `store/layers.ts` (discriminated `Layer` union + pure list helpers). **Render worker composites the *visible* layers** into one scene (≤1-layer fast path = old `renderOnce`; ≥2 composite offscreen + present via a quad), replacing the single-`currentScene()` path — the retrofit you don't want to eat at M4. `recompute()` **auto-seeds one `volume` layer** (the only change-point) so the existing field-selector + window/level panel still drive it; per-layer opacity = a `uLayerOpacity` uniform. New `upsertLayer`/`removeLayer`/`setComposite` messages; `app/layerSync.ts` bridges store→worker (`computed` carries field data, `layers` carries structure). Renamed the mislabeled `layersPanel`→`colormapPanel`. **Deferred:** `ColormapBinding` + per-layer color/window → 5b; rail + Layers panel + per-layer settings + colorbar → M4; live N≥2 swapchain present + mixed-camera (world-space MPR) slices → M4.
- [x] 5b. **`ColormapBinding`** — the color-mapping a layer references (`layer.colormapBindingId`), webpic-native: `{id, field, colormap: ColormapId, window {center,width}, scale: linear|log|symlog}` in `@schema/colormap.ts` (the authority; the canonical `WindowLevel` moved here too). *Not* a mirror of magviz's bare-string prototype. Layers share or split bindings deliberately (slice + volume on one `|B|` binding, or distinct). Store gains a `colormapBindings` registry + `setBinding{Colormap,Window,Scale}` intents; `recompute()` seeds one binding and **retargets** it (field + full-range window; colormap/scale preserved) on field switch. **All three scales honored on the GPU** — `render/normalization.ts` is a uniform-selected TSL graph (in-shader log/symlog with ε-floors) with a pure-TS `windowedT` twin under analytic test. `app/layerSync.ts` adds a **bindings channel** for live colormap/window/scale; `colormapPanel.ts` drives the selected layer's binding. **Land before v0.1 freeze** so persisted-session round-trips don't break; `merge`/GC/the 2-colormap soft-warn + a magviz import adapter (`data/import/magviz.ts`) wait for the **M4 multi-layer UI** / M6.
- [x] 6. Min-max mipmap empty-space skipping — **gate contingency**, profiled then shipped **default-off**: it *regresses* the representative space-filling Gaussian flux-rope `|B|` (~24 ms vs ~13.5 ms — the skip-grid fetch + branch costs ~2× traffic for ~no skips). Built/tested/dormant: `render/minMaxGrid.ts` (per-brick min/max, **R32F + nearest** — Metal-safe: `float32-filterable` gates only *linear*) + `render/brickStep.ts` (TS twin of the WGSL brick-DDA); the `skipEmptySpace` path snaps skips back onto the fixed lattice so it's pixel-equal to the fixed march (GPU parity test). **Enable only for genuinely sparse data** (vacuum, isolated ropes); the gate stays open for space-filling `|B|` (needs step/resolution caps, LOD, or an M2 Pro re-run — see the exit gate).
- [x] 7. Gradient + Phong central-difference shading (~20 lines WGSL — shape-perception win) — a **per-layer toggle, off by default** for quantitative work (the lit "surface" is a TF-dependent opacity isosurface, not physical). 6 taps/step, so **gated on per-sample opacity AND a `uShade` uniform** — transparent samples skip the taps and default-off costs one uniform compare, leaving the gate's default path unchanged; `setShading()` flips it live (no rebuild, no re-upload). Central-difference gradient + two-sided headlight Blinn-Phong in `raymarchScene.ts`'s shared `accumulate` (lights both march paths); pure twin `render/shading.ts` under analytic test. `shaded: boolean` on the volume `Layer` + `setLayerShading` intent, threaded through `messages.ts`/worker/`layerSync` + a Phong checkbox in the colormap panel. **Render-local — *not* `coordinates/gradient`**; a shading normal isn't a physics operator.
- [x] 8. `timestamp-query` diagnostics panel (+ `performance.now` fallback) — the instrument for the exit gate + the M2.6 contingency call. **Deviation from "just wire `gpu/profiler.ts`":** that profiler times an *owned* compute pass and can't reach three's internal render encoder, so render-pass GPU time uses **three's built-in tracking** (`WebGPURenderer({trackTimestamp:true})` → `resolveTimestampsAsync('render')`); `gpu/profiler.ts` left untouched for M3 compute. New `render/frameTimer.ts` (two backends behind one iface); the worker posts a `frameTiming` `RenderWorkerResponse`, and `setContinuous` flips the on-demand loop to every-frame for sustained measurement. `ui/panels/diagnosticsPanel.ts`: rolling-mean readout vs the **8 ms gate**, honestly-labeled clock (timestamp-query vs "≈ wall-clock"). **Metal finding:** macOS render-pass timestamp-query advertises support but intermittently returns garbage durations, so the timer is **adaptive** — demotes to wall-clock for the session after 4 consecutive bad reads. **Follow-on:** feed the panel a 256³ volume to make the empty-space-skipping call (→ M2.6).
- [x] 9. **Time cursor + scrub control** — the prefetch prerequisite. Store gains `currentStep`/`availableSteps` + `setStep` (ignores an out-of-domain step, identity-skips a no-op) + a supporting `setAvailableSteps` (the reader→store seam: replaces the domain, snaps the cursor to the nearest survivor). `ui/panels/timePanel.ts` — a scrub `RangeControl` that walks **indices** into `availableSteps` (robust to sparse/non-contiguous steps), dispatches `setStep`, reflects external steps, disables on a 0/1-step domain. Registered as the `time` panel (`field → time → colormap → diagnostics`). **Cursor is inert pre-M2.10** — `setStep` updates the store but nothing re-reads yet (the streaming core wires that).
- [x] 10a. **Time-series streaming — data side** (the render-side ping-pong is 10b). Streaming is mandatory at 256³ (~64 MiB/field/step, ring depth N≈1–5, can't hold 256). A synthetic multi-step reader (`data/readers/synthetic.ts` — a time-varying Gaussian flux rope behind the `SimulationReader` protocol; step 0 ≡ `createSyntheticDataset` so the seed and streamed step 0 agree, no flash) gives the worker a real `openSimulation`/read/abort path. A pure, tested ring orchestrator (`data/stream.ts`) holds N decoded steps around the cursor, prefetches **±1** (index-space, sparse-robust), aborts in-flight reads the cursor scrubs past, evicts farthest-first, re-reads on scrub-back. The data worker reads + computes the scalar **off the main thread** (`computeField`) and transfers it straight to the render worker over a **paired `MessageChannel`** (protocol in `data/streamMessages.ts`, since `workers` can't import `render`). **Division that avoids double-sourcing:** `setStep` doesn't recompute, so `computed` is silent during a scrub → `layerSync` (initial + field-switch) and the data-worker stream (scrub) never fight.
- [x] 10b. **Time-series streaming — render side** — replace `swapLayerField`'s per-swap scene rebuild with an in-place **ping-pong `Data3DTexture`** double-buffer in `render/volumeTexture.ts` (`setField`): pack the new step into the inactive texture and swap the shared TSL node's value — a **bind-group swap, not a pipeline recompile**. The second buffer is allocated lazily on first swap; the CPU image is rewritten in place (no per-step 64 MiB realloc). `swapLayerField` does the in-place swap, **retains the live field** so a device-restore rebuild reproduces the on-screen step, and **falls back to a full rebuild** only when the scene declines (shape change, or a `skipEmptySpace` raymarch whose acceleration grid would go stale). Normalization stays the **binding's fixed window across steps** (no per-step autoscale flicker — the correct time-series behavior). Sustained 256³ scrub on base M2: p50 ≈ 22 ms (over the 8 ms gate — already open for space-filling `|B|` per M2.6, unchanged by 10b). **Deferred:** OPFS scrub-back caching (optional; the synthetic re-read is cheap + deterministic).
- [x] 11. **Prefetch prediction `data/prefetch.ts`** — built/tested/**dormant** (M2.6 pattern: no stall observed — the synthetic reader's reads are cheap, so dumb ±1 only stalls behind a slow *real* reader; the predictor's payoff is gated on a future Zarr/OPFS profile). `createScrubPredictor` is a pure, time-injected (`observe(step, nowMs)`) twin of the ring's symmetric prefetch: EWMA index-velocity (α=0.7) → direction, windowed mean speed (window=8) → count(1–5), 250 ms debounce on a *reversal* (stationary→moving commits at once), 2 s idle → reset to ±1; `plan(center)` biases the wanted set ahead of travel (+ one trailing step for single-backtrack insurance). Wired via a new injectable `plan?` on `StreamRing` (`wantedSteps` delegates + filters to in-domain; `setCursor` now always retains the cursor in the abort set) — default path byte-identical, all 8 stream tests green. **Activation seam (deferred):** the data worker still drives ±1; flipping it on also needs `capacity ≥ MAX_COUNT+2` (=7) or `evict()`'s distance sort drops the farthest-*ahead* prefetch first — ~448 MiB vs ~320 MiB resident at 256³, the cost that gates turning it on. Reached via `@data/prefetch.ts` (not barreled — matches its sibling `stream.ts`). Unit + fuzz scrubber (seeded Park–Miller LCG, no `Math.random`) + ring-integration fuzz in `tests/prefetch.test.ts`.
- [x] 12. **Eager pipeline compile** (production — gates the first-frame budget). The **warm itself already shipped** with the compositor (`renderer.compileComposite` → `compileAsync(scene, camera)` per layer, `setRenderTarget` re-asserted before each await so pipelines land in the right per-context cache; driven from `layerRegistry.replace` → `warmScene` → `host.warmComposite` inside `upsertLayer`, before the first `requestRender`). So 12's remaining piece was the **compile-complete ack**: a new `layerCompiled` `RenderWorkerResponse` (echoes the layer `id`; posted from the worker's `upsertLayer` in a `finally`, so it fires on success, supersede, *or* warm failure → the pill never strands). `app/layerSync` now owns a flat **`render`** loading phase — `beginLoading` on the upsert post, `endLoading` on the `layerCompiled` ack (`handleCompiled`, routed from `main.ts` onmessage) — completing the "main's synchronous layerSync covers that case" seam `streamingBridge` already assumed, and **bridging the cold-start gap** (the boot pill ends on `ready`/the perf mark, but the volume's warm+first-paint lands later, so the `render` pill carries through: `webpic` → `opening dataset` → `preparing render` → content). Composes with streaming's `open`/`step` phases (scrub path). Tests: `main.test` loading-phase choreography (+ the `layerCompiled` ack), a `layerSync` begin/end test, and a `layerCompiled` arm in the two GPU browser readback switches. **Deferred:** the spinner is coalesced (one volume layer in v0.1); a per-`id` pill waits for multi-layer (M4).
- [x] 13. **Shader HMR** (dev-only — tree-shaken from prod, build-verified). Edit WGSL/TSL in `raymarchScene.ts` → the volume re-renders with **no reload, no lost pose, no 64 MiB re-upload**. `createRaymarchScene` now splits the preserved GPU state (`buildRaymarchGraph` — volume texture + colormap LUT + live uniforms) from the TSL/WGSL graph (`buildRaymarchMaterial`), so `rebuildShader(build)` swaps **only `mesh.material`** over the same graph. Flow: a dev-only `shaderHmr` Vite plugin's `handleHotUpdate` fires a custom `webpic:shader-hmr` event + returns `[]` (suppresses the full-reload the worker-only import would otherwise trigger); `app/shaderHmr.ts` (behind `import.meta.hot`, dynamic-imported) forwards it as a `rebuildShader` `RenderWorkerRequest`; the worker (behind `import.meta.env.DEV`) re-imports `raymarchScene.ts?t=<timestamp>` fresh via `volume/shaderReload.ts`, fans the fresh `buildRaymarchMaterial` through `registry.rebuildShaders` (volume-only — `"rebuildShader" in scene`), re-warms the composite, repaints. A bad edit (parse/compile error) is reported and keeps the prior shader. Prod build confirmed to strip the plugin (`apply:'serve'`), the bridge (no chunk in `index`), and the worker re-import path (`loadFreshRaymarchBuilder`/`?t=` absent from `worker`). **Not** vanilla worker `import.meta.hot` (the worker has no HMR client) — message-driven, so the `GPUDevice` + uploaded `Data3DTexture` + store-owned pose all survive. (Prereq M2.4a/4b store-owned pose — met.)
- [x] 14. **Package-split checkpoint** — **decided: stay single-package for v0.1** (DAG already enforced 3 ways; revisit at M6/embed, and if ever split, collapse math-y layers into one `@webpic/core` rather than promoting all 18). See `docs/DESIGN.md` §Package shape.
- [x] **Exit gate:** 256³ × 256-step @ ≤8 ms/frame raymarch on M2 Pro — **decision: 256³ is the v0.1 volume ceiling; 512³ + LOD bricks deferred to v0.2** (the gate's prescribed contingency). Measured on **base** Apple M2 (10-core GPU — *not* the M2 Pro target) via `scripts/profile-raymarch.ts` (production preview, headed Chrome, 256³ synthetic |B|, 256 steps/ray, default fixed march, early-α 0.98, ~1 MP surface = 1280×720 @ dpr 1): sustained settled per-frame **p50 ≈ 12.4 ms** (min 11.5 / max 13.6), wall-clock (over-reads true GPU). Over the 8 ms gate on base M2, but the M2 Pro target carries ~1.6–1.9× the GPU throughput (16–19 vs 10 cores) → extrapolates to **~6.5–7.5 ms**, plausibly meeting the gate at ~1 MP — *unconfirmed* (no M2 Pro on hand; the instrument flags a base-M2 over-read as ambiguous). At Retina dpr 2 (~4 MP) the settled frame exceeds budget on any current Apple GPU; interaction stays bounded by the dpr cap (2) + the render-scale governor. Per M2.6, empty-space skipping can't help space-filling |B|; the real levers (settled step cap, resolution/render-scale cap, LOD bricks) are quality trade-offs deferred with 512³. **Scrub without stalls: met** — re-confirmed on base M2 via `scripts/verify-streaming-render.ts` (sustained bouncing scrub: streamed steps reach the GPU, main thread responsive at max longtask 111 ms < 120 ms budget, no faults; continuous full-quality p50 ≈ 12 ms), backing M2.10b + the green streaming unit suites; the interaction path (qualityController march-coarsen + render-scale drop during gestures, streaming ring + ping-pong) keeps scrubbing fluid regardless of the settled cost. *Instrument fixes (both gate scripts had drifted against the floating-window/top-bar UI refactor):* the Diagnostics panel — the gate's measurement UI — had been orphaned (docked shell mounts nothing by default) → re-homed into the Developer window; both scripts' surface report corrected to reconstruct the true drawing buffer (CSS client × DPR) over the stale post-transfer `canvas.width`; `verify-streaming-render.ts`'s scrub driver moved from the old docked Time pane to the top-bar step buttons and its canvas locator scoped to `#app canvas` (colorbar/swatch added sibling canvases).

## M3 — WebGPU compute + parity
Goal: GPU compute backend agreeing with pypic goldens. (WASM backend deferred → M9.)
- [ ] 1. `compute/backends/webgpu` for `field.{magnitude,curl,divergence}` — kernels authored as standalone WGSL in `shaders/` (shared w/ rustpic, tested vs the `coordinates/` TS twin); imports `shaders` + `gpu`
- [ ] 2. `tests/tolerances.ts` — per-kernel, per-precision tolerance table (honest f32/f16 bounds)
- [ ] 3. Cross-backend equivalence harness: `ts` vs `webgpu` per kernel
- [ ] 4. `gen-fixtures.ts` — pypic golden outputs checked into `tests/fixtures/v1/`
- [ ] 5. `gen-synthetic.ts` — Orszag-Tang, Harris, GEM analytical fields
- [ ] **Exit gate:** TS-vs-WebGPU agree within tolerance against pypic goldens

## M4 — Field lines + instance-first UI  *(first to cut if M2 slips)*
Goal: cancellable GPU streamlines matching pypic's Dormand-Prince traces, plus the multi-layer UI (rail + Layers panel) that volume+slice+fieldlines finally justify. *UI half (7–9) can defer independently of the trace half (1–6); if M4 is cut, the layer model (M2.5a) falls back to the minimal window/level panel.*
- [ ] 1. `numerics/integrators`: port pypic `dormand_prince_step` + `i_step_controller` (CPU reference)
- [ ] 2. `numerics/tracing.ts`: `trace_field_line[s]_adaptive` + `TerminationReason` (mirror `pypic.traces`)
- [ ] 3. WGSL streamline compute kernel (standalone `shaders/` asset, shared w/ rustpic) — **DP5(4) + PI step control**, one invocation/seed
- [ ] 4. Raycast seed picking against slice/volume bounds
- [ ] 5. `Line2`/`LineSegments2` indirect-draw render (no per-frame readback)
- [ ] 6. `AbortSignal` cancellation through the dispatcher (cancel mid-trace). **Store-side seam already in place:** `store/simulation.ts` `recompute()` owns a per-call `AbortController` and threads `signal` into `computeField`, aborting a superseded compute (the data worker already passes one through). M4 wires the trace dispatcher onto this existing origin rather than building cancellation from scratch.
- [ ] 7. **Instance-first Layers UI** (fixed translucent overlays + rail — the "viz is the product" *look*, drag/resize/persist deferred to M7/v0.2). Rail (left, translucent): per-primitive **add**-buttons with hover-preview/click-pin panels listing existing instances + "Add new"; tool buttons (reductions/selections/diagnostics/theme); a **Layers** toggle. **Layers panel** (collapsible overlay): one row per layer = **eye** (show/hide) + **gear** (open settings) + reorder. Embed hides/toggles overlays — same components, fewer of them. (DESIGN §UI.)
- [ ] 8. **Per-layer settings component** — *one* component, two entry points (rail create-panel vs. Layers gear), so a layer is never configured in two places. Fields/domain, colormap (M2.5b binding), window/level, opacity, draw order, remove, + kind-specific (slice axis/position, fieldline seeds).
- [ ] 9. **Colorbar** (draggable + hideable, lifted from magviz — a one-off drag handler, not the M7 window manager). Content = the ≤2 *distinct* `ColormapBinding`s among visible layers; the stack + 2-colormap soft-warn from M2.5b's registry.
- [ ] **Exit gate:** cancellable DP5(4) traces match pypic golden traces within tolerance; volume + slice + field lines co-display as managed layers (eye/opacity/colormap per layer)

## M6 — Writers + export
Goal: persist derived fields + screenshots; lock bundle budgets.
- [ ] 1. `data/writers/zarr.ts` — Zarr v3 writer (derived-field caching, modifications)
- [ ] 2. **Reduction-provenance round-trip** — preserve `attrs["reduction"]` verbatim (`axis`, `op`, `result_kind`, `weight`, `length_axes`)
- [ ] 3. `simulation.toml` round-trip (`attrs.run` preferred; `attrs.simulation_toml` fallback)
- [ ] 4. PNG screenshot (`canvas.toBlob('image/png')`)
- [ ] 5. Theme switcher (cycle bundled themes; saved via OPFS)
- [ ] 6. TypeDoc from public `@webpic/embed` exports
- [ ] 7. `size-limit` CI gate
- [ ] 8. **Top bar** (thin, translucent) — consolidates the global chrome: dataset name, the M2.9 timestep scrubber, camera/projection, export (M6.4). Coarse/infrequent; not per-layer. (DESIGN §UI.)
- [ ] **Exit gate:** 1.0 MB embed / 1.6 MB app gzipped; reduction round-trip preserved

---

## Backlog (v0.2+)
- [ ] **M5 — Particles:** `hyparquet`, instanced billboards, GPU cull/sort, density-binning fallback, `particleAccessMode` enum, DuckDB dynamic import
- [ ] **M7 — UI polish:** floating **window manager** (free-drag/resize/persist/focus — magviz's `windowManager` + `draggablePanels`; v0.1 overlays are fixed-position translucent, this makes them user-rearrangeable), command palette (Cmd-K), remappable-shortcuts UI, comparison view, high-contrast theme
- [ ] **M8 — HDF5 + LIC + oblique:** `h5wasm`, LIC compute pass, oblique clip planes
- [ ] WebCodecs video export (`VideoEncoder` + `MediaStreamTrackProcessor`)
- [ ] **Remote client** — `remote/client.ts` against pypic.server Arrow-IPC WebSocket; mirror `SubscribeRequest`/`*Spec`/`ReductionSpec`
- [x] PWA manifest — `public/manifest.webmanifest` + icons (qlmanage-rendered from `public/icon.svg`) + iOS standalone meta in `index.html`; guarded by `tests/manifest.test.ts`. Offline service-worker still deferred (needs a data source anyway).
- [ ] **M9 (contingent)** — WASM backend (`@rustpic/plasma-wasm`) when rustpic ships it; extend cross-backend parity to TS vs WASM vs WebGPU

## Post-v1.0
- [ ] Spherical/cylindrical raymarcher path · [ ] WebTransport (when pypic.server adopts) · [ ] Marching-cubes isosurfaces · [ ] Pre-integrated TF · [ ] Plugin / extensibility API
