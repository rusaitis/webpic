# webpic — TASKS

Check off `[ ]` → `[x]` as steps land. The `/tasks` skill summarizes progress; `/task` reads this file.
Full rationale for every step: **`docs/DESIGN.md`** (read on demand — not auto-loaded).

**v0.1 = M0–M4 + M6.** v0.1 deliberately skips M5 (particles → v0.2): the sequence is M4 → M6.
**Current milestone: M2.**

> **Scope-risk decision point — M2 exit:** if M2 slips materially, first cut
> **M4 (streamlines)** to v0.2 — slice + volume is already a useful viewer. Cut M6 only if the Zarr
> writer / `attrs["reduction"]` round-trip proves harder than expected (export gates the analyzer).

---

## M0 — Foundation
Goal: toolchain + schema codegen + scaffolds boot; OffscreenCanvas worker alive.
- [x] 1. Vite + TS strict (`tsconfig.base.json`) + Biome + Vitest; `npm` scripts wired
- [x] 2. `src/<layer>/` skeleton (schema, containers, coordinates, numerics, gpu) + path aliases (`@schema/*`, …)
- [x] 3. `scripts/check-boundaries.ts` (ts-morph) — layer-DAG enforcement, runs in CI ahead of `biome ci`
- [x] 4. Schema codegen: `scripts/codegen/{bundle,emit,render-schema,render-aliases,render-recipes,render-registry}.ts` (pypic `export bundle` → Zod + TS types via `npm run gen`)
- [x] 5. `gpu/`: device acquire, capability probe, `device.lost` recovery, timestamp-query + `performance.now` fallback profiler
- [x] 6. OffscreenCanvas-on-Worker scaffold (`transferControlToOffscreen`); main-vs-worker frame-parity test
- [x] 7. Theme loader — reads `pypic/plotting/themes/*.toml` `[webpic]` block (7 themes, incl. `anuppuccin-light`)
- [x] 8. OPFS cache w/ `navigator.locks`; writes confined to `data.worker.ts` (worker-only sync handle; Safari <26)
- [x] 9. Background backend microbench (seeds dispatcher calibration; scores cached in OPFS by GPU adapter)
- [x] **Exit gate:** cold-start page paint <500 ms; first frame <1500 ms (M2 Pro Chrome stable). Measured by `npm run perf:gate` (`scripts/perf-gate.ts` — Playwright drives system Chrome stable headed, prod preview). Cold on base M2 (conservative proxy for M2 Pro): page paint 146 ms, first frame 210 ms — both well under budget. Re-validate at M2 once the raymarcher + `compileAsync`-at-boot replace the scaffold triangle.

## M1 — Static slice
Goal: read a Zarr store and render one themed orthogonal slice of `|B|`.
- [x] 1. `data/readers/zarr.ts` (zarrita.js) implementing `SimulationReader` + `FieldListingReader`
- [x] 2. `data/readers/_registry.ts` — `registerReader` + `openSimulation()` (confidence-ranked)
- [x] 3. `data/stagger.ts` — destagger Yee components to the co-located grid on load (`destaggerToColocated`, wired into the Zarr reader behind a no-op fast path); typed-array-backed `FieldDataset` consumed verbatim
- [x] 4. `derived/magnitude.ts` registered under canonical `'|B|'`; TS backend in `compute/backends/ts`
- [x] 5. `render`: one orthogonal `Mesh` (`NodeMaterial` sampling `uVolume`) with themed colormap
- [x] 6. `computeField('|B|', dataset)` wired store → compute → render end-to-end
- [x] 7. Schema-parity test (`pypic schema diff` + regenerated-vs-checked-in Zod) + additive-compat test
- [x] **Exit gate:** `computeField('|B|')` end-to-end; schema-parity + additive-compat green. Verified: typecheck/boundaries/lint clean, 188 tests pass, schema-parity + additive-compat green vs live pypic (`WEBPIC_PYPIC_PARITY=1`).

## M2 — Volume + perf gate
Goal: single-scalar volume raymarcher hitting the perf gate; time-series scrub.
- [x] 1. Single-pass volume raymarcher (TSL `NodeMaterial` + `wgslFn`, render-local — not a shared kernel); analytic ray-box, early-α (≥0.98) termination. `render/raymarchScene.ts` (TSL `Loop` + `wgslFn` `hitBox` + front-to-back composite); `rayBox.ts` TS twin + Node tests; worker `showVolume` + real-GPU smoke. Slice stays app default until the M2.2 UI toggle.
- [x] 2. **`ui/` scaffold** — hideable shell panels + dependency-free `Pane`/`Folder`/`Binding` controls facade (slider/select/checkbox/text, ported from `magviz/src/ui/controls`, no `tweakpane`) + `ControlDescriptor` schema-aware binder. `ui` dispatches typed store intents only — never imports `render` (DAG-enforced); embed renders without `@webpic/ui`. One smoke per panel. *(foundation for M2.3 `RangeControl`, M2.8 diagnostics panel, M6.5 theme switcher)* Rewritten to webpic's callbacks-out / `set()`-in contract (no two-way binding), `ownerDocument` DI, `.webpic-*` classes; native `<select>` + basic linear slider (full `RangeControl` → M2.3). `installUi()` mounts a themed docked shell + wired field selector (options = store `availableFields`; `selectField` intent) + `layers`/`diagnostics` placeholders; `toggleUi` shortcut. `happy-dom` `*.dom.test.ts` project for DOM smokes (15 tests). typecheck/boundaries/lint/build clean, 212 tests pass.
- [x] 3. Transfer-function texture (256×1 `rgba16float`) + window/level via **owned `RangeControl`** — its `{center,width}` output is the canonical range form the M2.5b `ColormapBinding` stores (no separate `min/max`). `render/transferFunction.ts` bakes the colormap LUT from `colormapColor` (RGB; alpha=1, reserved for the M2.7 opacity TF), sampled in both scenes; window/level becomes `uWindowCenter`/`uWindowWidth` uniforms (full-range default = old normalization) with an in-place `setWindowLevel` so dragging never re-uploads the volume (new `setWindowLevel` render message, no field buffer). Store gains `dataRange` + `windowLevel {center,width}` (reset to full range per field) + `setWindowLevel` intent. **Owned `RangeControl`** ported from magviz (full linear/log/symlog/interval + `rangeMath`, `ownerDocument` DI, `.webpic-range*`, callbacks-out/set()-in) → `addRangeControl` facade. The **Layers** placeholder is now a real panel: a linear interval window/level control (interval↔`{center,width}` at the store seam; M2.5b adds colormap + scale pick). `colormapNode.ts` retired. typecheck/lint/boundaries/build clean, 269 tests pass (+ rangeMath port, TF LUT, store window/level, RangeControl + Layers DOM smokes).
- [x] 4a. **Render loop + store-owned camera pose** — the interactive-camera foundation M2.1 punted (camera is static inside `createRaymarchScene`, `lookAt(0,0,0)`; rendering is one-shot `renderFrame` on data change). Lift the camera out of the scene factories — pose lives in the store and streams to the worker (`StoreToRender — camera pose`, DESIGN §443, unbuilt; `messages.ts` has no camera channel yet). Worker applies the pose to the current scene's camera and drives a continuous frame loop (one-shot → rAF). gl-matrix `Vec3` pose state store-side; THREE camera stays in `render/`. Unblocks honest per-frame perf measurement (M2.8 + exit gate need a real loop) and the M2.13 HMR "keep pose" prerequisite. **Done:** camera lifted into `render/camera.ts` (`createPerspectiveCamera`/`createOrthographicCamera`/`applyPose` + `DEFAULT_POSE`); the three scene factories no longer own a camera; the worker owns one perspective (pose-driven, volume) + one ortho (screen-aligned, slice/boot) camera. Store gains non-null `cameraPose` — orbit-spherical `{target, azimuth, elevation, distance}` typed via the existing `@schema` `Vec3` (**no gl-matrix** — deferred to M4 per CLAUDE.md; the readonly tuple already crosses store↔render and suits zustand's reference-equality) — plus a `setCameraPose` intent, a new `setCameraPose` `StoreToRender` message, and an `app/main.ts` subscription mirroring window/level. Worker drives an **on-demand rAF loop** (`needsRender` dirty flag; synchronous-paint fallback when `requestAnimationFrame` is absent, i.e. Node) that replaces the per-message `renderOnce`; a `readbackInFlight` guard stops the loop from corrupting the test-only `renderFrame` readback; the `continuous`-override seam is left for M2.8. Pose drives the **perspective camera only** (orbiting a 2D ortho slice is meaningless), so it is inert on the default slice until 4b's pointer input + the volume toggle land. typecheck/boundaries/lint/build clean, 279 tests pass (+`camera.test.ts`: ortho determinism, spherical→cartesian, `DEFAULT_POSE` reproduces the legacy `(1.4,1.1,1.6)` view); perf gate green on real Chrome (`navigator.gpu` yes, cold first frame 299 ms).
- [x] 4b. **Orbit/turntable controls + pointer input** — the interaction on top of 4a. Pointer events land on the main-thread container (the `OffscreenCanvas` is transferred to the worker) → camera deltas → store intent → `StoreToRender`. Orbit (azimuth/elevation/target) + turntable (constrained up-axis); `navMode` selects. Owned controls — no `OrbitControls`-as-dep unless it earns its keep. Makes M2.7 Phong actually assessable (rotate to perceive shape). Bottom-left chrome rides here too: projection toggle (persp/ortho) + coordinate readout + camera gnomon (lifted from magviz). Can slip past 4a without blocking the loop/HMR work. **Done:** orbit/dolly/pan as **pure pose helpers** in `store/camera.ts` (`orbitPose`/`dollyPose`/`panPose` + `DISTANCE_MIN/MAX` + module-private sensitivities; node-tested, fresh-object-per-call for `subscribeWithSelector`). A thin `ui/pointerCamera.ts` (`ui → store` only, no `render`) attaches pointer/wheel listeners to the main-thread `<canvas>` — `transferControlToOffscreen` moves only the drawing surface, so the element still gets DOM events — left-drag orbits, shift/middle-drag pans, wheel dollies, all dispatched as `setCameraPose`; `app/main.ts` wires it **guarded** (`typeof canvas.addEventListener === "function"`) so the headless handshake test's fake canvas is untouched. Live HUD `ui/cameraChrome.ts`: a bottom-left readout (az/el/dist/target) + a **CSS-3D gnomon** driven straight from the pose (no second renderer, no worker round-trip — the pose is angle-parameterized), hiding with the global UI toggle. **Render side unchanged** — 4a's worker already handles `setCameraPose` and its on-demand rAF loop coalesces a drag's many posts into one paint. **Deferred (clean seams):** the `navMode` selector + a free-trackball orbit (fixed-up spherical makes orbit ≡ turntable; a real trackball needs a quaternion pose), and the **projection toggle** (needs a pose-driven matched-frustum ortho camera in the worker; rides with the volume toggle / M2.5a compositor). Inert-until-volume, like 4a: the pose drives only the perspective/volume camera, so orbiting won't *move* the screen-aligned default slice — verified by unit + DOM tests, not visible rotation; the HUD *is* immediately live. typecheck/boundaries/lint/build clean, 298 tests pass (+19: pose-helper math, pointer→intent dispatch, HUD reflect/hide/dispose); perf gate green on real Chrome (`navigator.gpu` yes, cold first frame 373 ms).
- [x] 5a. **Layer model** (instance-first foundation) — store `layers` registry: an ordered list of `{id, kind: volume|slice|fieldlines|particles, inputs (field/domain), colormapBindingId, visible, opacity, params}` + intents (add/remove/select/reorder/setVisible/setOpacity). **Render worker composites the *visible* layers** into one scene (draw order + per-layer opacity), replacing the single-`currentScene()` path — *this* is the retrofit you don't want to eat at M4: a hardcoded one-field→one-scene render path is painful to unwind once volume+slice+fieldlines must co-display (the same argument the binding below makes, but stronger — it touches the render worker). Pre-M4 the app auto-creates one layer (active field as volume/slice) so the existing field-selector + window/level panel still drive it; the full multi-layer **UI** lands M4. Rename the mislabeled `layersPanel` (it holds window/level, not a layer list). **Done:** `store/layers.ts` — discriminated `Layer` union (`kind` slice/volume/fieldlines/particles, restated `SliceAxis`, `colormapBindingId` null until 5b) + pure list helpers (fresh-array, identity-on-noop); `simulation.ts` gains `layers`/`selectedLayerId` + the six intents (per-store monotonic ids) and **auto-seeds one `volume` layer** in `recompute()` (the only change-point), with `selectField` re-pointing the selected layer's field. Render worker drops the `slice`/`volume` singletons for a `Map<id, scene>` + ordered `composite`; `currentScene()` → `paintItems()`; `renderer.renderComposite`/`readCompositePixels` (`compositeInto` = clear-once + per-item `clearDepth`; ≤1-layer fast path = old `renderOnce`, ≥2 composites offscreen + presents via a quad — backgrounds nulled, one renderer clear color). Per-layer opacity = a `uLayerOpacity` uniform (raymarch final-α; slice `opacityNode`). New `upsertLayer`/`removeLayer`/`setComposite` messages (drop `show*`); window/level + colormap stay **global/`"inferno"`** (→ 5b). App `layerSync.ts` bridges store→worker: `computed` carries field data (one transfer/change), `layers` carries structure (remove + composite). Renamed `layersPanel`→`colormapPanel` (`installColormapPanel`, `colormap` registry key + all theme `default-panels`). typecheck/boundaries(77)/lint/build clean; **318 tests** pass (+20: 15 layer helpers/intents, 5 layerSync) + 2 GPU-skipped compositor browser tests, 13 GPU-skipped total; perf gate green on real Chrome (`navigator.gpu` yes, cold first frame 272 ms) and a live orbit smoke confirmed the volume renders + rotates. **Deferred:** `ColormapBinding` + per-layer color/window → 5b; rail + Layers panel + kind toggle + per-layer settings + colorbar → M4; live N≥2 swapchain present + per-layer compute buffers + mixed-camera (world-space MPR slices) → M4.
- [x] 5b. **`ColormapBinding`** — the color-mapping a layer references (`layer.colormapBindingId`), in webpic-native shape (typed `FieldName` + `ColormapId`, **window/level** `{center,width}` matching M2.3's `RangeControl`, discriminated `scale: linear|log|symlog`), *not* a field-for-field mirror of magviz's prototype (its bare-string `colormap`/`units`/`min,max` stay out). Layers share or split bindings deliberately (slice + volume on one `|B|` binding, or distinct). v0.1 ships the minimal registry; `merge`/GC/the 2-colormap soft-warn wait for the **M4 multi-layer UI**. **Land before v0.1 freeze** so persisted-session round-trips don't break. Magviz session round-trip is *not* the driver: boundary import adapter (`data/import/magviz.ts`, decoding magviz `{min,max,log,logFloor}` → window/level) at M6 session-export / v0.2. **Done:** `@schema/colormap.ts` is the authority — `ColormapBinding {id, field, colormap: ColormapId, window {center,width}, scale}` + `COLORMAP_IDS`/`COLOR_SCALES`/`DEFAULT_COLORMAP`; the canonical `WindowLevel` moved here too (store + `render/{normalization,messages}` import it; only the portable `ui/controls/rangeMath` copy stays restated), and `render/colormap.ts` types its coeff table by `ColormapId` (compile-time schema↔fit parity). Store gains a `colormapBindings` registry (`store/colormap.ts` pure record ops, identity-on-noop) + `setBinding{Colormap,Window,Scale}` intents; `recompute()` seeds one binding with the auto-layer and **retargets** it (field + full-range window, colormap/scale preserved) on field switch; the global `windowLevel`/`setWindowLevel` are gone. **All three scales honored on the GPU** — `normalization.ts` is a uniform-selected TSL graph (`uCenter`/`uWidth`/`uScaleMode`, in-shader log/symlog with ε-floors) with a pure-TS `windowedT` twin under analytic test; scenes gained idempotent `setColormap` (LUT rebake) + uniform `setScale`. Wire: `upsertLayer` carries `scale`; the old global `setWindowLevel` message → per-layer `setLayerColormap`; `app/layerSync.ts` resolves a layer's binding into the upsert and a new **bindings channel** posts the live colormap/window-drag/scale (skipping freshly-seeded bindings — those ride the upsert); `app/main.ts` drops its window subscription. UI `colormapPanel.ts` drives the *selected layer's* binding (colormap select + scale select that re-bakes the window track with a log `min>0` guard + window `RangeControl`). typecheck/lint/boundaries(89)/build clean; **337 tests** pass (+19: binding ops, `windowedT` linear/log/symlog, store seeding/retarget, layerSync upsert+bindings channel, panel colormap/scale/window), 13 GPU-skipped.
- [x] 6. Min-max mipmap empty-space skipping — **gate contingency**; profiled first (CLAUDE.md "profile before tuning"), implemented, and shipped **default-off** because the profile showed it *regresses* the representative field. **Findings (`scripts/profile-raymarch.ts`, real Chrome, base M2 @ 1.64 MP / dpr 1):** the 256³×256-step fixed march sustains ~13.5 ms — over the 8 ms gate — but it's *space-filling*: the synthetic Gaussian flux-rope `|B|` keeps values above the opacity floor across nearly the whole domain (envelope > skip-ε out to r≈1.16; the box only reaches r≈1.41), so almost no brick is skippable. With skipping forced on, every occupied step pays an extra skip-grid fetch + branch (~2× memory traffic) for ~no skips → **~24 ms, +1.7× slower** and jittery (the exact failure the task flagged: "the win is real only for sparse fields"). **Built (correct, tested, dormant):** `render/minMaxGrid.ts` — coarse per-brick min/max as a pure typed-array reduction in the volume-texture layout (1-voxel halo so the max bounds *trilinear* samples; NaN→min fallback), uploaded as **R32F + nearest** (Metal-safe — `float32-filterable` gates only *linear*; max-only, min reserved); `render/brickStep.ts` — the tested TS twin of the WGSL `brickAdvance` brick-DDA (rayBox.ts ↔ hitBox precedent). `raymarchScene.ts` now has two march paths sharing one `accumulate`: the default fixed `Loop`, and an opt-in (`skipEmptySpace`) two-level coarse-skip / fine-march that **snaps skips back onto the fixed lattice** so occupied samples are *output-equivalent* to the fixed march (the GPU parity test asserts pixel-equality skip-vs-fixed). `?n=` synthetic-size override (`src/main.ts`) feeds the profiler the gate workload. typecheck/lint/boundaries(93)/build clean; **372 tests** pass (+`minMaxGrid` 8, `brickStep` 6; +1 GPU-skipped parity); default path re-profiled at ~14 ms (no regression). **Gate still open for space-filling `|B|`** — empty-space skipping is the wrong lever; it needs steps/resolution caps, LOD, or an M2 Pro re-run (see the exit gate). Enable `skipEmptySpace` for genuinely sparse data (vacuum, isolated ropes).
- [x] 7. Gradient + Phong central-difference shading (~20 lines WGSL — shape-perception win) — ships as a **toggle**, off by default for quantitative work (the lit "surface" is a TF-dependent opacity isosurface, not physical). Not free: 6 taps/step on a 256-step march, so gate the gradient on `sampleAlpha` or precompute a gradient volume lest it eat the 8 ms gate. Render-local — *not* `coordinates/operators/gradient`; a shading normal isn't a physics operator, don't dedupe them. **Done:** central-difference gradient (6 texture taps via a shared `sampleRawAt` with the same non-finite guard as the main sample) + two-sided **headlight** Blinn-Phong in `raymarchScene.ts`'s shared `accumulate`, so it lights both march paths. **Gated on per-sample opacity** `t.greaterThan(SHADE_T_FLOOR)` (the dt-free form of `sampleAlpha`, reusing `EMPTY_T = 1/255`) **AND** a `uShade` uniform — transparent samples skip the 6 taps, and default-off costs one uniform compare (the `If` body never runs), so the gate's default path is unchanged. `setShading()` flips `uShade` live (no rebuild, no 64 MiB re-upload). The lighting math has a pure twin `render/shading.ts` (`gradientToNormal` + `headlightShade`, the rayBox.ts ↔ hitBox precedent) under analytic test. **Toggle is a real per-layer property end-to-end:** `shaded: boolean` on the volume `Layer` (default false) + `setLayerShading` store intent + pure helper; `messages.ts` carries it on `upsertLayer` + a live `setLayerShading` message (volume-only, narrowed via `"setShading" in scene`); worker threads it through `LayerSource`/`buildScene` (device-restore-safe) and the new handler; `layerSync` sends it on upsert + diffs the structure channel for an existing layer's flip; a "Phong" checkbox in the colormap panel's new **Shading** folder drives the selected volume layer (disabled for slices/no-layer, reflects external flips). Render-local lighting normal stays *out* of `coordinates`. typecheck/lint/boundaries(97)/build clean; **386 tests** pass (+`shading` 9 incl. degenerate-gradient fallback + two-sided + monotonic-tilt, +layers `setLayerShading`, +layerSync upsert-carries/live-toggle/seeded-rides-upsert, +panel checkbox enable+toggle+reflect), 15 GPU-skipped; perf gate green on real Chrome (`navigator.gpu` yes, cold first frame 277 ms) — and the default render compiles the full shader *including* the dormant Phong `If` block, confirming the WGSL is valid on Metal.
- [x] 8. `timestamp-query` diagnostics panel (+ `performance.now` fallback) — the instrument for the exit gate + the M2.6 contingency call. **Deviation from "just wire `gpu/profiler.ts`":** that profiler's timestamp backend times an *owned* compute pass (`GPUComputePassTimestampWrites` + your own encoder) and can't reach three's internally-managed render encoder — so render-pass GPU time uses **three's built-in tracking** (`WebGPURenderer({trackTimestamp:true})` → `resolveTimestampsAsync('render')`, ms), `gpu/profiler.ts` left untouched for M3 compute. New render-local `render/frameTimer.ts` (two backends behind one iface); the worker brackets the raymarch composite in `renderTick`, posts a new `frameTiming` `RenderToStore` reply (`render/messages.ts`), and a `setContinuous` request flips the on-demand loop to every-frame for *sustained* measurement (the documented `worker.ts:123` seam). Store gains `frameTimeMs`/`frameTimeClock`/`isMeasuringContinuous` + `setFrameTiming`/`setMeasuringContinuous`; `app/main.ts` bridges the reply→store and the toggle→worker (guarded on `workerReady`). The M2.2 stub became a real `ui/panels/diagnosticsPanel.ts`: rolling-mean readout vs the **8 ms gate**, an **honestly-labeled clock** (timestamp-query vs "≈ wall-clock · incl. JS/queue"), and a "Measure (continuous)" toggle that resets on dispose (never strands the GPU-pegging loop). **Metal finding:** macOS render-pass timestamp-query advertises support but intermittently returns garbage durations (negative / 1e8 ms), so the timer is **adaptive** — plausibility-gated, demoting to wall-clock for the session after 4 consecutive bad reads (tolerates the "first-frame garbage" wart, survives wholesale-broken drivers). typecheck/boundaries(91)/lint/build clean; **353 tests** pass (+16: frameTimer mode/plausibility/demotion, store diagnostics intents, `rollingMean`, panel DOM smoke), 13 GPU-skipped; perf gate green (cold first frame 240 ms). Real-Chrome `scripts/verify-diagnostics.ts` confirms `frameTiming` flows end-to-end with a sane GPU-clock value + correct label. **Follow-on (closes the original `/task` M2.6 target):** feed the panel a 256³ volume and read the sustained raymarch ms against the 8 ms gate to make the empty-space-skipping call.
- [x] 9. **Time cursor + scrub control** — the prefetch prerequisite. Store gains `currentStep`/`availableSteps` + a `setStep` intent; `ui` gains a scrub `RangeControl`. Reader substrate is ready (`readTimestep`/`availableTimesteps`, multi-step tested) — today the store has no time cursor at all, so this lands before any streaming. **Done:** store gains `currentStep`/`availableSteps` + `setStep` (moves the cursor; ignores an out-of-domain step, identity-skips a no-op) and a supporting `setAvailableSteps` (the reader→store seam — replaces the domain, snaps the cursor to the nearest survivor, identity-skips an identical list); `setDataset` now tracks the loaded `dataset.step` and seeds a 1-element domain for a direct/synthetic load while leaving a reader-populated list intact. New `ui/panels/timePanel.ts` — a single-mode scrub `RangeControl` that walks *indices* into `availableSteps` (every grip position maps to a real step, so it's robust to sparse/non-contiguous steps with no snapping; the readout shows the step value, and index ≡ step for the common contiguous-from-zero domain), dispatches `setStep`, reflects external steps via `set()`, rebuilds on a domain change, and disables itself with a 0/1-step domain. Registered as the `time` panel (`field → time → colormap → diagnostics`) in `DEFAULT_WEBPIC_CONFIG` (webpic's own fallback — the vendored pypic theme TOMLs lead/lag independently). **Cursor is inert pre-M2.10** — `setStep` updates the store but nothing re-reads yet; no worker channel (the streaming core wires that). typecheck/lint/boundaries(99)/build clean; **394 tests** pass (+8: 5 store cursor — domain seeding, reader-list survival, setStep guards/identity-skip, snap-on-domain-change; 3 panel DOM — disable/enable, scrub→setStep + external reflect, sparse index→step readout), 15 GPU-skipped.
- [x] 10a. **Time-series streaming — data side** (split from M2.10; the render-side ping-pong is 10b). The exit-gate "scrub without stalls" clause: streaming is mandatory at 256³ (~64 MiB/field/step, ring depth N≈1–5, can't hold 256). **Done:** a synthetic multi-step reader (`data/readers/synthetic.ts` — a time-varying Gaussian flux rope behind the `SimulationReader` protocol; `synthetic://fluxrope?n&steps` handle, `availableTimesteps`/`readTimestep` with `AbortSignal` + `fields`, step 0 ≡ `createSyntheticDataset` so the main seed and the streamed step 0 agree — no flash) gives the worker a real `openSimulation`/read/abort path; `app/syntheticDataset.ts` now delegates to its step-0 generator. A pure, fully-tested ring orchestrator (`data/stream.ts`) holds N decoded steps around the cursor, prefetches **±1** (index-space over the domain — robust to sparse steps), aborts in-flight reads the cursor scrubs past, evicts farthest-first, and re-reads consumed/evicted steps on scrub-back ("hold until upload, transfer on upload, re-read on scrub-back"). The data worker (`workers/data.worker.ts`, extended alongside its OPFS-write path) drives the ring: on `setCursor` it reads + computes the scalar **off the main thread** (`computeField`) and transfers it straight to the render worker over a **paired `MessageChannel`** (`render/worker.ts` gains a `pair` handler + a port `streamStep` → factored `swapLayerField` seam, reusing the retained `LayerSource`'s look). Wire/protocol in the `data` layer (`streamMessages.ts`) since `workers` can't import `render`; `app/main.ts` spawns + pairs the data worker (only when a `streamSource` is given), relays `opened`→`setAvailableSteps`, and drives `currentStep`→`setCursor` / `activeField`→`setActiveField`. **Division that avoids double-sourcing:** `setStep` doesn't recompute, so `computed` is silent during a scrub → `layerSync` (initial + field-switch) and the data-worker stream (scrub) never fight. typecheck/lint/boundaries(113)/build clean; **415 tests** pass (+21: 8 ring fuzz/abort/evict, 9 synthetic reader, 2 render `pair`/`streamStep`, 2 bootstrap pairing/cursor), 15 GPU-skipped. Real-Chrome `scripts/verify-streaming.ts` (128³): scrubbing changes the rendered frame **and** the main thread stays responsive (1 incidental long task, 84 ms < 120 ms budget — not the per-step pattern an on-main 128³ compute would produce), proving the read+compute is off-main.
- [ ] 10b. **Time-series streaming — render side**: replace `swapLayerField`'s per-swap scene rebuild (today's path, retained from 10a) with an in-place **ping-pong `Data3DTexture`** double-buffer (upload to the inactive texture, swap the uniform — reuse geometry/material/TF, no 64 MiB rebuild per step), then feed the diagnostics panel a sustained scrub and read raymarch ms against the 8 ms gate. OPFS caching of decoded steps on scrub-back is an optional refinement here (10a re-reads via the synthetic reader, which is cheap + deterministic).
- [ ] 11. **Prefetch prediction `data/prefetch.ts`** — profile-then-tune, added only if the task-10 dumb ±1 stalls ("profile before tuning"). EWMA direction (window=8, α=0.7) + magnitude→count(1–5) + 250 ms debounce-on-flip + stationary→±1. Fuzz scrubber in `tests/prefetch.test.ts`.
- [ ] 12. **Eager pipeline compile** (production — gates the first-frame budget). `renderer.compileAsync(scene, camera)` (the renderer method — there is no `material.compileAsync`) warms the heavy raymarch pipeline *before its first `renderOnce`*, off the critical path, so the first frame doesn't hitch on driver compile. Not literally "at boot": `worker.ts:init()` only builds the triangle then; warm each heavy scene in `upsertLayer` (the volume/slice layer-build seam; coincides with boot only if a default dataset auto-loads). Post a compile-complete reply (DESIGN §444 `RenderToStore`) so the UI can drop its spinner.
- [ ] 13. **Shader HMR** (dev-only — `import.meta.hot` is tree-shaken from prod). Edit WGSL/TSL in `raymarchScene.ts`, see it re-render **without a reload or losing state**. *Prerequisite:* camera pose must be **store-owned** (DESIGN §443 `StoreToRender — camera pose`) + an interactive camera must exist — today the camera is static and built *inside* `createRaymarchScene`, so a scene rebuild resets the very pose this promises to keep (built by M2.4a/4b). Mechanism: message-driven in-place rebuild (main watches the file → `rebuildShader` message → worker swaps the material), **preserving the `GPUDevice` + uploaded `Data3DTexture`** (don't re-upload 64 MiB per keystroke) and re-applying the stored pose — not vanilla worker `import.meta.hot`.
- [ ] 14. **Package-split checkpoint** — decide: promote `src/<layer>/` → `@webpic/<layer>` packages vs. collapse math-y layers into one `@webpic/core` (see `docs/DESIGN.md` §Package shape)
- [ ] **Exit gate:** 256³ × 256-step dataset @ ≤8 ms/frame raymarch on M2 Pro — else defer 512³ to v0.2 + LOD bricks; scrub without stalls

## M3 — WebGPU compute + parity
Goal: GPU compute backend agreeing with pypic goldens. (WASM backend deferred → M9.)
- [ ] 1. `compute/backends/webgpu` for `field.{magnitude,curl,divergence}` — kernels authored as standalone WGSL in `shaders/` (shared w/ rustpic, tested vs the `coordinates/` TS twin); imports `shaders` + `gpu`
- [ ] 2. `tests/tolerances.ts` — per-kernel, per-precision tolerance table (honest f32/f16 bounds)
- [ ] 3. Cross-backend equivalence harness: `ts` vs `webgpu` per kernel
- [ ] 4. `gen-fixtures.ts` — pypic golden outputs checked into `tests/fixtures/v1/`
- [ ] 5. `gen-synthetic.ts` — Orszag-Tang, Harris, GEM analytical fields
- [ ] **Exit gate:** TS-vs-WebGPU agree within tolerance against pypic goldens

## M4 — Field lines + instance-first UI  *(first to cut if M2 slips)*
Goal: cancellable GPU streamlines matching pypic's Dormand-Prince traces, plus the multi-layer UI (rail + Layers panel) that volume+slice+fieldlines finally justify. *UI half (7–9) can defer independently of the trace half (1–6); if M4 is cut, the layer model (M2.5a) falls back to the minimal window/level panel.*
- [ ] 1. `numerics/integrators`: port pypic `dormand_prince_step` + `i_step_controller` (CPU reference)
- [ ] 2. `numerics/tracing.ts`: `trace_field_line[s]_adaptive` + `TerminationReason` (mirror `pypic.traces`)
- [ ] 3. WGSL streamline compute kernel (standalone `shaders/` asset, shared w/ rustpic) — **DP5(4) + PI step control**, one invocation/seed
- [ ] 4. Raycast seed picking against slice/volume bounds
- [ ] 5. `Line2`/`LineSegments2` indirect-draw render (no per-frame readback)
- [ ] 6. `AbortSignal` cancellation through the dispatcher (cancel mid-trace)
- [ ] 7. **Instance-first Layers UI** (fixed translucent overlays + rail — the "viz is the product" *look*, drag/resize/persist deferred to M7/v0.2). Rail (left, translucent): per-primitive **add**-buttons with hover-preview/click-pin panels listing existing instances + "Add new"; tool buttons (reductions/selections/diagnostics/theme); a **Layers** toggle. **Layers panel** (collapsible overlay): one row per layer = **eye** (show/hide) + **gear** (open settings) + reorder. Embed hides/toggles overlays — same components, fewer of them. (DESIGN §UI.)
- [ ] 8. **Per-layer settings component** — *one* component, two entry points (rail create-panel vs. Layers gear), so a layer is never configured in two places. Fields/domain, colormap (M2.5b binding), window/level, opacity, draw order, remove, + kind-specific (slice axis/position, fieldline seeds).
- [ ] 9. **Colorbar** (draggable + hideable, lifted from magviz — a one-off drag handler, not the M7 window manager). Content = the ≤2 *distinct* `ColormapBinding`s among visible layers; the stack + 2-colormap soft-warn from M2.5b's registry.
- [ ] **Exit gate:** cancellable DP5(4) traces match pypic golden traces within tolerance; volume + slice + field lines co-display as managed layers (eye/opacity/colormap per layer)

## M6 — Writers + export
Goal: persist derived fields + screenshots; lock bundle budgets.
- [ ] 1. `data/writers/zarr.ts` — Zarr v3 writer (derived-field caching, modifications)
- [ ] 2. **Reduction-provenance round-trip** — preserve `attrs["reduction"]` verbatim (`axis`, `op`, `result_kind`, `weight`, `length_axes`)
- [ ] 3. `simulation.toml` round-trip (`attrs.run` preferred; `attrs.simulation_toml` fallback)
- [ ] 4. PNG screenshot (`canvas.toBlob('image/png')`)
- [ ] 5. Theme switcher (cycle bundled themes; saved via OPFS)
- [ ] 6. TypeDoc from public `@webpic/embed` exports
- [ ] 7. `size-limit` CI gate
- [ ] 8. **Top bar** (thin, translucent) — consolidates the global chrome: dataset name, the M2.9 timestep scrubber, camera/projection, export (M6.4). Coarse/infrequent; not per-layer. (DESIGN §UI.)
- [ ] **Exit gate:** 1.0 MB embed / 1.6 MB app gzipped; reduction round-trip preserved

---

## Backlog (v0.2+)
- [ ] **M5 — Particles:** `hyparquet`, instanced billboards, GPU cull/sort, density-binning fallback, `particleAccessMode` enum, DuckDB dynamic import
- [ ] **M7 — UI polish:** floating **window manager** (free-drag/resize/persist/focus — magviz's `windowManager` + `draggablePanels`; v0.1 overlays are fixed-position translucent, this makes them user-rearrangeable), command palette (Cmd-K), remappable-shortcuts UI, comparison view, high-contrast theme
- [ ] **M8 — HDF5 + LIC + oblique:** `h5wasm`, LIC compute pass, oblique clip planes
- [ ] WebCodecs video export (`VideoEncoder` + `MediaStreamTrackProcessor`)
- [ ] **Remote client** — `remote/client.ts` against pypic.server Arrow-IPC WebSocket; mirror `SubscribeRequest`/`*Spec`/`ReductionSpec`
- [ ] PWA manifest
- [ ] **M9 (contingent)** — WASM backend (`@rustpic/plasma-wasm`) when rustpic ships it; extend cross-backend parity to TS vs WASM vs WebGPU

## Post-v1.0
- [ ] Spherical/cylindrical raymarcher path · [ ] WebTransport (when pypic.server adopts) · [ ] Marching-cubes isosurfaces · [ ] Pre-integrated TF · [ ] Plugin / extensibility API
