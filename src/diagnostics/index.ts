// Reserved layer (pypic-mirrored DAG): physics diagnostics — energy spectra, conservation checks.
// Pure leaf, typed arrays in/out. STAGED: activates with the analyzer leg (DESIGN §Data layer).
export {};
