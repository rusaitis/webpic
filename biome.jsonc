{
  "$schema": "./node_modules/@biomejs/biome/configuration_schema.json",
  "vcs": {
    "enabled": true,
    "clientKind": "git",
    "useIgnoreFile": true
  },
  "files": {
    "ignoreUnknown": true
  },
  "formatter": {
    "enabled": true,
    "indentStyle": "space",
    "indentWidth": 2,
    "lineWidth": 100
  },
  "linter": {
    "enabled": true,
    "rules": {
      "preset": "recommended",
      // Ratchets: each ceiling sits at today's worst value, so nothing new may be worse than the
      // worst thing already here. They only ever fall — lower them in the commit that splits the
      // offender. Lowering either by one fails today: complexity 25 is `bootstrap`'s composition
      // root, lines 210 is `createRangeControl`'s interaction state machine. CLAUDE.md §Lifecycle
      // & shape asks for 20/150; reaching 20 means splitting thirteen more bodies, most of them
      // single machines (the DP5(4) loop, the destagger pass, a canvas paint), so treat it as a
      // direction rather than a near target — and never buy a lower number with a suppression.
      // A body that is long or branchy *by construction* takes a biome-ignore naming that reason;
      // six do — four in src/, plus a browser probe and an instrument.
      "complexity": {
        "noExcessiveCognitiveComplexity": {
          "level": "error",
          "options": { "maxAllowedComplexity": 25 }
        },
        "noExcessiveLinesPerFunction": { "level": "error", "options": { "maxLines": 210 } }
      },
      "nursery": {
        "noFloatingPromises": "error",
        "noMisusedPromises": "error",
        "useExhaustiveSwitchCases": "error"
      },
      "correctness": {
        "noUnusedVariables": "error",
        "noUnusedFunctionParameters": "error"
      },
      "suspicious": {
        "noConsole": {
          "level": "error",
          "options": { "allow": ["warn", "error"] }
        }
      },
      "style": {
        "noParameterAssign": "error",
        "noRestrictedImports": {
          "level": "error",
          "options": {
            "patterns": [
              {
                "group": ["three", "three/**"],
                "message": "three.js is render-only — no THREE.* outside src/render/ (see docs/DESIGN.md §Layered dependency DAG)."
              }
            ]
          }
        }
      }
    }
  },
  "javascript": {
    "formatter": {
      "quoteStyle": "double",
      "semicolons": "always",
      "trailingCommas": "all"
    }
  },
  "assist": {
    "enabled": true,
    "actions": {
      "source": {
        "organizeImports": "on"
      }
    }
  },
  "overrides": [
    {
      "includes": ["src/render/**"],
      "linter": { "rules": { "style": { "noRestrictedImports": "off" } } }
    },
    {
      // A test body is linear and reads top to bottom; length there is not the smell the rule is for.
      "includes": ["scripts/**", "tests/**", "**/*.test.ts"],
      "linter": { "rules": { "complexity": { "noExcessiveLinesPerFunction": "off" } } }
    },
    {
      // An instrument reports to a terminal; there is no @schema/log sink in scripts/.
      "includes": ["scripts/**"],
      "linter": { "rules": { "suspicious": { "noConsole": "off" } } }
    },
    {
      "includes": ["**/*.generated.ts"],
      "linter": { "enabled": false },
      "assist": { "actions": { "source": { "organizeImports": "off" } } }
    },
    {
      "includes": ["**/*.generated.json", "tests/fixtures/**/*.json"],
      "formatter": { "enabled": false }
    }
  ]
}
